# Local secrets — never commit
config.js

# OS / editor cruft
.DS_Store
node_modules/
package-lock.json
package.json
test-hydra.js

# Claude Code local settings (may contain keys from recorded commands)
.claude/
